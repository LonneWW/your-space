import { FloatingTooltipDirective } from './floating-tooltip.directive';

describe('FloatingTooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new FloatingTooltipDirective();
    expect(directive).toBeTruthy();
  });
});
